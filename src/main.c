/*
 * Initial main.c file generated by Glade. Edit as required.
 * Glade will not overwrite this file.
 */

#ifdef HAVE_CONFIG_H
#  include <config.h>
#endif

#include <gtk/gtk.h>

#include "interface.h"
#include "support.h"
#include "callbacks.h" 

/*
 * Initial main.c file generated by Glade. Edit as required.
 * Glade will not overwrite this file.
 */

/*int main (int argc, char *argv[])
{
  GtkWidget *FitVerse;

#ifdef ENABLE_NLS
  bindtextdomain (GETTEXT_PACKAGE, PACKAGE_LOCALE_DIR);
  bind_textdomain_codeset (GETTEXT_PACKAGE, "UTF-8");
  textdomain (GETTEXT_PACKAGE);
#endif

  gtk_set_locale ();
  gtk_init (&argc, &argv);

  add_pixmap_directory (PACKAGE_DATA_DIR "/" PACKAGE "/pixmaps");

  // Création de la fenêtre principale
  FitVerse = create_FitVerse ();
  gtk_widget_show (FitVerse);

  // Récupérer le GtkCombo
  GtkWidget *combo = lookup_widget(FitVerse, "GCIA_comboType");

  //  Connecter le signal "changed" sur GtkEntry (saisie manuelle)
  GtkEntry *entry = GTK_ENTRY(GTK_COMBO(combo)->entry);
  g_signal_connect(G_OBJECT(entry), "changed",
                   G_CALLBACK(on_GCIA_comboType_entry_changed),
                   NULL);

  //  Connecter le signal "select-child" sur GtkList (choix dans la liste)
  GtkList *list = GTK_LIST(GTK_COMBO(combo)->list);
  //  On passe l’entry comme user_data pour éviter l’erreur list->parent
  g_signal_connect(G_OBJECT(list), "select-child",
                   G_CALLBACK(on_GCIA_comboType_list_selected),
                   entry);

  gtk_main ();
  return 0;
}*/
int main (int argc, char *argv[])
{
  GtkWidget *FitVerse;
  GtkWidget *TIEC;  
#ifdef ENABLE_NLS
  bindtextdomain (GETTEXT_PACKAGE, PACKAGE_LOCALE_DIR);
  bind_textdomain_codeset (GETTEXT_PACKAGE, "UTF-8");
  textdomain (GETTEXT_PACKAGE);
#endif
  gtk_set_locale ();
  gtk_init (&argc, &argv);
  
  
  add_pixmap_directory ("pixmaps");
  
  
  add_pixmap_directory (PACKAGE_DATA_DIR "/" PACKAGE "/pixmaps");
  
  
  FitVerse = create_FitVerse ();
  gtk_window_set_default_size(GTK_WINDOW(FitVerse), 1000, 750);
  gtk_widget_set_size_request(FitVerse, 400, 500);
  gtk_widget_show (FitVerse);
  
  TIEC = create_TIEC();
  gtk_window_set_default_size(GTK_WINDOW(TIEC), 980, 800);
  gtk_widget_set_size_request(TIEC, 300, 200);
  gtk_widget_show (TIEC);

  

  GtkWidget *combo = lookup_widget(FitVerse, "GCIA_comboType");
  
  
  GtkEntry *entry = GTK_ENTRY(GTK_COMBO(combo)->entry);
  g_signal_connect(G_OBJECT(entry), "changed",
                   G_CALLBACK(on_GCIA_comboType_entry_changed),
                   NULL);
  
  
  GtkList *list = GTK_LIST(GTK_COMBO(combo)->list);
  
  g_signal_connect(G_OBJECT(list), "select-child",
                   G_CALLBACK(on_GCIA_comboType_list_selected),
                   entry);
  
  gtk_main ();
  return 0;
}


